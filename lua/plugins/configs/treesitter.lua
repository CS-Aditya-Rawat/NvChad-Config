local options = {
  ensure_installed = { "lua" , "python", "typescript-language-server"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
